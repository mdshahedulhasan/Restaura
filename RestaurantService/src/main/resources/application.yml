spring:
  datasource:
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://localhost:5432/restaurants
    username: postgres
    password: root

  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
  data:
    jdbc:
      dialect: postgresql